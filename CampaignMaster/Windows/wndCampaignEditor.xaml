<Window x:Class="CampaignMaster.Windows.wndCampaignEditor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CampaignMaster"
        xmlns:controls="clr-namespace:CampaignMaster.Controls"
        xmlns:vm="clr-namespace:CampaignMaster.ViewModels"
        Width="750"
        Height="100" WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True" Background="Transparent" Topmost="True">
    <Window.DataContext>
        <vm:vmCampaignEditor/>
    </Window.DataContext>
    <Grid>
        <Border Background="Black" Opacity="0.6" CornerRadius="10"/>
        <Grid>
            <Border x:Name="MaskBorder" Background="White" Margin="15" CornerRadius="10"/>
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>
                <Grid.OpacityMask>
                    <VisualBrush Visual="{Binding ElementName=MaskBorder}"/>
                </Grid.OpacityMask>
                <TextBox HorizontalAlignment="Left" VerticalAlignment="Center" Background="Transparent" BorderThickness="0" Margin="15,0,0,0" FontWeight="Bold" FontSize="45" Foreground="Black" Text="{Binding Campaign.Name}"/>

                <Button Grid.Column="1" Margin="10" Background="PeachPuff" BorderThickness="0" Command="{Binding CommandSaveCampaign}" Click="Save_Click">
                    <Viewbox>
                        <TextBlock Text="" FontFamily="Segoe UI Symbol"/>
                    </Viewbox>
                </Button>
            </Grid>
        </Grid>
    </Grid>
</Window>
