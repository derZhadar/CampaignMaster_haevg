<Window x:Class="CampaignMaster.Windows.wndQuit"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CampaignMaster.Windows"
        xmlns:controls="clr-namespace:SamCorp.WPF.Controls;assembly=SamCorp.WPF"
        mc:Ignorable="d"
        Width="420"
        Height="460" WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True" Background="Transparent" Topmost="True">
    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/CampaignMaster;component/Resources/Images/Backgrounds/MenuBackground.png"/>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="40*"/>
            <RowDefinition Height="60*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Bottom" Text="Beenden?" Foreground="{StaticResource PaperBrown}" FontFamily="Bradley Hand ITC" FontWeight="Bold" FontSize="26"/>
        <Border Grid.Row="0" VerticalAlignment="Bottom" Margin="20,0,20,0" BorderThickness="0,0,0,2" BorderBrush="#FF301D17"/>

        <TextBlock Grid.Row="1" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20" Text="Wie viele Tage sind vergangen?" Foreground="{StaticResource PaperBrown}" FontFamily="Bradley Hand ITC" FontWeight="Bold" FontSize="44" TextWrapping="Wrap"/>

        <Grid Grid.Row="2" Margin="0,0,0,20" Width="400" Height="200">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="txtDays" Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="0" Grid.RowSpan="2" Margin="5,5,5,15" Foreground="{StaticResource PaperBrown}" Text="0" VerticalAlignment="Center" HorizontalAlignment="Right" FontSize="140"/>

            <controls:SamButton Grid.Column="2" Grid.Row="0" CornerRadius="100" Foreground="{StaticResource PaperWhite}" Background="{StaticResource PaperBrown}" BorderBrush="{StaticResource PaperWhite}" BorderThickness="4" Click="ButtonIncreaseDayClick">
                    <TextBlock Margin="0,-5,0,0" FontFamily="Segoe UI Symbol" Text="&#xE0E4;" FontSize="62" FontWeight="Bold"/>
            </controls:SamButton>
            <controls:SamButton Grid.Column="2" Grid.Row="1" CornerRadius="100" Foreground="{StaticResource PaperWhite}" Background="{StaticResource PaperBrown}" BorderBrush="{StaticResource PaperWhite}" BorderThickness="4" FontFamily="Segoe UI Symbol" Content="&#xE0E5;" FontSize="62" FontWeight="Bold" Click="ButtonDecreaseDayClick"/>

            <controls:SamButton Grid.Column="3" Grid.Row="0" VerticalAlignment="Bottom" Height="70" Width="70" CornerRadius="100" Foreground="DarkGreen" Background="{StaticResource PaperWhite}" BorderBrush="DarkGreen" BorderThickness="4" FontFamily="Segoe UI Symbol" Content="&#xE10B;" FontSize="43" FontWeight="Bold" Click="ButtonOkClick"/>
            <controls:SamButton Grid.Column="3" Grid.Row="1" VerticalAlignment="Top" Height="70" Width="70" CornerRadius="100" Foreground="DarkRed" Background="{StaticResource PaperWhite}" BorderBrush="DarkRed" BorderThickness="4" FontFamily="Segoe UI Symbol" Content="&#xE10A;" FontSize="43" FontWeight="Bold" Click="ButtonCancelClick"/>
        </Grid>

    </Grid>
</Window>
