<views:ctlViewBase x:Class="CampaignMaster.Controls.Views.viewNotes"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                   xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                   xmlns:local="clr-namespace:CampaignMaster"
                   xmlns:misc="clr-namespace:CampaignMaster.Misc"
                   xmlns:controls="clr-namespace:CampaignMaster.Controls"
                   xmlns:vm="clr-namespace:CampaignMaster.ViewModels"
                   xmlns:views="clr-namespace:CampaignMaster.Controls.Views"
                   Background="{StaticResource View.Background}"
                   Caption="Notes"
                   mc:Ignorable="d" 
                   d:DesignHeight="1600" d:DesignWidth="2400">
    <views:ctlViewBase.DataContext>
        <vm:vmNotes/>
    </views:ctlViewBase.DataContext>
    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/CampaignMaster;component/Resources/Images/Backgrounds/NotesBoard.jpg"/>
        </Grid.Background>

        <!--<Grid.RowDefinitions>
            <RowDefinition Height="105"/>
            <RowDefinition Height="30"/>
            <RowDefinition/>
        </Grid.RowDefinitions>-->
        <!--<Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>-->
        
        <!--<Border Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="0" Grid.RowSpan="2" Background="Transparent"/>

        <controls:ctlCalendar Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="0" BorderBrush="Gray" BorderThickness="0,0,1,1"/>

        <Border Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1" Background="#FF464646">
            <Viewbox>
                <TextBlock Text="Notes" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"/>
            </Viewbox>
        </Border>-->
        <Grid Margin="290,140,580,180">
            <InkCanvas Background="Transparent" Strokes="{Binding Notes}">
                <InkCanvas.DefaultDrawingAttributes>
                    <DrawingAttributes FitToCurve="True"/>
                </InkCanvas.DefaultDrawingAttributes>
            </InkCanvas>
            <!--<Border Grid.Column="1" Grid.Row="2" IsHitTestVisible="False">
                <Border.Background>
                    <DrawingBrush TileMode="Tile" ViewportUnits="Absolute" Viewport="20,20,20,20">
                        <DrawingBrush.Drawing>
                            <GeometryDrawing>
                                <GeometryDrawing.Geometry>
                                    <RectangleGeometry Rect="0,0,100,100"/>
                                </GeometryDrawing.Geometry>
                                <GeometryDrawing.Pen>
                                    <Pen Brush="Gray" Thickness="1"/>
                                </GeometryDrawing.Pen>
                            </GeometryDrawing>
                        </DrawingBrush.Drawing>
                    </DrawingBrush>
                </Border.Background>
            </Border>-->
        </Grid>
        
    </Grid>
    </views:ctlViewBase>